@model IEnumerable<EntityLayer.Concrete.About>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<div class="site-section bg-light">
    <div class="container">
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-6 order-md-2">
                    <img src="@item.AboutImage1" alt="Image" class="img-fluid">
                </div>
                <div class="col-md-5 mr-auto order-md-1">
                    <h2>Biz Kimiz?</h2>
                    <p>@Html.Raw(item.AboutContent1)</p>
                </div>
            }
        </div>               
    </div>
</div>

@*@Html.Action("MeetTheTeam", "About")*@

<div class="site-section bg-light">
    <div class="container">
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-6">
                    <img src="@item.AboutImage2" alt="Image" class="img-fluid">
                </div>
                <div class="col-md-5 ml-auto">
                    <h2>Neler Yapıyoruz?</h2>
                    <p class="mb-4">@Html.Raw(item.AboutContent2)</p>
                </div>
            }
        </div>
    </div>
</div>

@Html.Action("Footer", "About")